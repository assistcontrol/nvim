if not AW.has('mini.animate') then return end

require('mini.animate').setup {
    -- Scroll
    scroll = { enable = false },
    -- Windows
    open   = { enable = false },
    close  = { enable = false },
}
